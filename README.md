Instructions for parsing .dem currently:
1. Insure demo file is in backend/src/dem_parser
2. Start .venv "source ./.venv/bin/activate
3. run "python3 movementcsv.py <demofile>
4. See new output folder with json inside. Git will ignore output folder